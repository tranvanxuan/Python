# tra ve mot so nguyen co kich thuoc chi dinh (in bits)

import  random
# tra ve so nguyen co kich thuoc 8 pcs
print(random.getrandbits(8))

#cu phap random.getrandbits(n)
    # n can thiet so chi dinh kich thuoc